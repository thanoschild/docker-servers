# Bind to all interfaces (or specify only the needed ones)
bind 0.0.0.0

# Require a password for all connections
requirepass admin

# Persist data to disk
dir /data
dbfilename dump.rdb

# Enable AOF persistence (optional but safer)
appendonly yes
appendfilename "appendonly.aof"
